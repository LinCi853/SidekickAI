# 更新日志

---

## v0.0.8 — 2026-08-01

> 本版本重点：白板引擎迁移至 Excalidraw（MIT 可商用）；品牌色系从砖红切换为靛蓝并系统性修复对比度；恢复截图到白板功能。

### 重要更改

- **白板引擎迁移：tldraw → Excalidraw**
  原白板组件 tldraw 采用专有许可证，存在商用合规风险。本版本整体迁移至 Excalidraw（MIT），数据模型从卡片级 API 重构为 Excalidraw scene snapshot，遗留 v1 卡片数据自动迁移。CSP 配置同步移除 `cdn.tldraw.com` 白名单。

- **品牌色系迁移：砖红 → 靛蓝**
  品牌主色从暖砖红切换为靛蓝（`--primary` = `#4F46E5`、`--ring` = `#6366F1`），与 UI 2.0 设计方向对齐。brand 色阶完整替换为 Indigo 50–900，品牌渐变统一为 `#6366F1 → #4338CA`，亮 / 暗双主题一致。

### 新功能

- **截图 AI 页面到白板**（恢复）
  右键顶栏标签页 →「页面操作」→「截图到白板」，将当前 AI 页面截图推送至进阶面板白板。适配 v3 Excalidraw 架构：`webview.capturePage()` 捕获 → 存为 `whiteboard-asset://` 磁盘资源 → 主进程打开进阶面板并切到白板 tab → 注入为 Excalidraw image 元素并自动滚动到视口。图片宽度超过 400px 时按比例缩放，随机偏移避免多张截图重叠。

### 体验优化

- **对比度系统性修复**：11 处「浅色背景 + 白字」场景（用户气泡、主按钮渐变、AppSwitcher 图标、提示词库标签与分段选中态、数据导出预设选中态、供应商模型标签等）对比度从 ~1.4:1 提升至 4.7:1（亮色）/ ~7:1（暗色），达到 WCAG AA 标准。
- **红系反馈色收敛**：错误 / 危险红统一为 `--destructive` 单一来源，`--danger` 作为其别名，消除 `#ef4444` 与 `#e57373` 双值漂移；`--window-close`（窗口控制红）、`--highlight-recording`（录制状态红）作为语义专用变体独立保留。
- **警告徽章对比度修复**：快捷键冲突徽章从「白字 + 琥珀底」（~2:1）改用专用令牌 `--warning-badge-bg` / `-fg`（亮色深棕字、暗色提亮琥珀 + 深字），双主题一致。
- **字体与圆角**：`--font-sans` 首位改 Exo 2（原 Geist 未加载）；`index.html` 加载 Playfair Display 使 `--font-serif` 生效；按钮圆角统一到 `--radius-sm`。
- **WebviewTab 导航修复**：webview `src` 仅在挂载时设置一次，避免 SPA 内部导航触发 React 重渲染导致 `ERR_ABORTED`。

### 问题修复

- 修复暗色主题下 `--danger` 仍使用亮色值的历史缺陷。
- 清理死兜底值 `var(--x, #错误值)` 与硬编码 `#fff`（统一改用 `--foreground-inverse` / `--destructive-foreground`）。

### 开源合规

- README 新增商标声明：本项目不隶属于所聚合的任何 AI 服务提供商，仅使用平台名称首字母 + 自定义渐变色作为视觉标识，未内置 / 分发任何平台 Logo 或商标图形。
- README 新增开源依赖许可证清单（Electron / React / Excalidraw / better-sqlite3 / Tiptap / Vite / Zustand 等 MIT，opencc-js Apache-2.0，lucide-react ISC，highlight.js BSD-3-Clause，DOMPurify MPL-2.0 / Apache-2.0）。

---

## v0.0.7 — 2026-07-26

> AiApp 概念统一重构为 AdvancedPanel（高级面板），并完成一轮死代码清理与通用基础设施抽取。

### 新功能

- **AiApp → AdvancedPanel 重构**：原「AI 应用」概念统一重命名为「高级面板」。新增 `AdvancedPanelView`、`AdvancedPanelSettingsPanel`、`AdvancedPanelGeneralSection` 与 `advanced-panel-window` 窗口工厂；移除旧的 `AiAppSettingsPanel` / `AiAppGeneralSection` / `AiProviderAppView` / `ai-app-window`。
- **Combobox 通用组件**：新增下拉组合框 UI 组件，统一选择交互。
- **webview-preload**：新增 webview 预加载脚本，为后续 webview 内部能力注入打基础。
- **平台检测增强**：`platform-detector` 新增 `isMobile()` 导出。

### 工程优化

- **死代码清理**：删除 headless 模块及 `puppeteer-core` 依赖；删除 `stt-cleaner`、`useSwipeNavigation`、`Card` / `ListItem` 等孤立文件；清理 20+ 未使用的 electron-api wrapper。
- **主进程基础设施抽取**：新增 `ipc-utils`（安全 IPC 包装）、`broadcast`（窗口广播）；扩展 `store-paths` 提供 SQLite / JSON store 基础设施；拆分 `voice-ipc` 为 `downloader` + `zip-extractor` 模块；重构 `window-factory` 抽取 webPreferences / 单例弹窗 / 脱离窗口生命周期工厂。
- **渲染层基础设施抽取**：新增 `useWindowMaximizedAndPinned` / `useIsNarrow` / `useEscToCloseWindow` 等 7 个 hook；新增 `StandaloneWindowHeader` / `VoiceProviderConfig` / `TitleBar` 组件；重构 SettingsPanel 聚合 props 传递。
- **CSS 通用样式表**：新增 `app-layout` / `forms` / `cards` 通用样式表；扩展 `TitleBar.css` 通用顶栏类。

### 问题修复

- 修复 `tsconfig.json` 未完整排除移动端源码导致 `tsc` 检查废弃代码的问题。

### 测试覆盖

本版本建立完整测试矩阵，共 **55 项** 测试用例，覆盖 13 个功能域：

| 功能域 | 测试项数 | 覆盖要点 |
|---|---|---|
| 窗口管理 | 5 | 弹出窗口单例、脱离窗口焦点追踪与位置记忆、最大化 / 置顶状态显示与监听无泄漏 |
| 笔记功能 | 5 | 新建切换、防抖自动保存（800ms）、关闭窗口 beforeunload 同步兜底、删除确认、IPC 错误处理 |
| 白板功能 | 7 | 新建切换、多图 / 多线 / 连接线绘制与保存、防抖自动保存（500ms）、关闭窗口兜底、IPC 错误处理 |
| 设置面板-通用 | 5 | 加载显示、修改保存、回滚、保存反馈提示、视觉一致性 |
| 设置面板-代理 | 2 | 草稿编辑 dirty 状态、连接测试结果块 |
| 设置面板-语音 | 4 | AI 语音 / 本地 STT / TTS 三配置独立编辑不互相干扰 |
| 语音模型下载与解压 | 3 | 下载进度与镜像源、PowerShell / AdmZip 双解压、卸载释放空间 |
| 独立窗口视图 | 7 | 提示词库 / 历史 / 引导页顶栏交互、ESC 层级关闭、输入框聚焦豁免、窄屏切换 |
| 聊天视图 | 2 | 窄屏切换（600px 阈值）、侧边栏切换按钮 |
| AI 应用管理 | 4 | 列表显示操作、编辑器表单、ESC 关闭、Provider 配置测试连接 |
| Profile 管理 | 2 | CRUD 操作、变更后窗口刷新广播 |
| CSS 视觉一致性 | 6 | 根容器布局、侧边栏列表项、空状态、Toast、卡片、顶栏图标按钮样式统一 |
| 应用启动与基础功能 | 3 | 正常启动、Alt+Q 窗口、UI 缩放变更广播 |

---

## v0.0.6 — 2026-07-17

> 本版本重点：安装包按 CPU 架构拆分发行，单架构体积下降超 50%；并补齐多开、自启、数据迁移、缓存 / 下载管理等实用能力。

### 重要更改

- **安装包按架构拆分**：x64 与 arm64 各出一个独立安装包，单架构体积从 180 MB 降至 78 MB（-56.6%），下载更快、占用更小。arm64 包在 x64 系统上运行时会弹窗提示，引导下载正确版本。

### 新功能

- **AI 应用多开与拖拽排序**：同一 AI 平台支持同时打开多个独立实例（如同时登录两个豆包账号），通过「复制」快速克隆配置，登录状态互不干扰；切换器、底部栏、设置面板均支持拖拽调整顺序。
- **开机自启动**：设置面板「通用」分区新增「开机自启动」开关，附带「静默启动」选项，自启后隐藏到托盘不弹出主窗口。
- **跨设备数据迁移**：设置面板「关于」分区可导出 / 导入完整数据包（含对话记录、登录状态和个性化设置），新设备导入后无需重新登录。导出支持细粒度选项，避免冗余体积。
- **缓存与下载管理**：新增缓存清理（手动 + 每日 / 每周 / 每月自动），仅清缓存保留登录；文本 / 代码文件可下载到指定目录，支持每次询问或自动保存。
- **浏览器文件拖拽导入**：将文件拖入窗口即可自动注入当前 AI 应用，三层兜底策略兼容智谱清言等延迟挂载输入框的平台。
- **Alt+Space 触发阈值可自定义**：设置面板「热键」分区新增次数配置（默认 6 次，范围 3-20），时间窗口随阈值自动放大，避免误触恢复窗口位置。
- **弹窗白名单机制**：连续 3 次拒绝弹窗后自动提示加入白名单，避免反复拦截登录 / OAuth 弹窗。

### 体验优化

- 清理设置面板内冗余的描述性说明文字，保留危险操作的二次确认提示与动态状态反馈。
- 设置面板折叠区块（AI 应用、设备预设）标题样式统一，展开 / 收起箭头反馈更清晰。
- 全部页面更新至 v0.0.6 版本标识。

---

## v0.0.5 — 2026-07-14

> 上一版本的稳定化版本，修复多项已发现问题，补齐窗口管理、快捷键和界面体验短板。

### 新功能

- **首次启动引导**：首次打开时弹出分步引导窗口，介绍核心亮点、窗口类型、快捷键速查和快速设置；老用户可点击「跳过」直接进入。
- **顶栏按钮自定义**：设置面板新增「顶栏」分区，可在 6 组按钮（UA 切换、后退、前进、主页、主题切换、置顶）中按需显隐。
- **新增标签快捷键**：Ctrl+T 新建标签（默认打开 DeepSeek），Ctrl+W 关闭当前标签。
- **新增导航快捷键**：F4 后退、F5 刷新、F6 前进，作用于当前标签。
- **F12 置顶全窗口通用**：自定义对话、历史搜索、提示词库、AI 应用编辑、使用引导等所有窗口均支持 F12 切换置顶，与主窗口一致。

### 体验优化

- UA 切换按钮升级为三态循环（自动 → 移动端 → 桌面端），每种状态配独立图标。
- AI 应用管理视图精简：默认进入对话列表，卡片新增「已打开」徽标和「打开 / 编辑」双按钮。
- AI 应用编辑视图整体优化：顶栏新增置顶、设置按钮；模型选择器样式更清晰；发送按钮与输入框对齐到同一栏；添加窗口边框避免与背景融合。
- 自定义对话窗口发送按钮样式统一，颜色更醒目，hover 状态更稳定。
- 使用引导布局优化：从 5 页合并为 4 页，消除底部空白，始终显示「跳过」按钮。
- 快捷键速查表全面同步，补齐 Ctrl+T/W、F4/5/6、F10 等条目。

### 问题修复

- **修复窗口无法缩小**：上一版本中窗口只能放大不能缩小，现已恢复正常。
- **修复拖动一次后调整手柄失效**：拖动窗口边缘一次后手柄卡死的问题已修复。
- **修复网页崩溃后无法恢复**：使用一段时间后点击主页或刷新报错的问题已修复，崩溃后可自动恢复。
- **修复提示词库编辑器弹窗默认显示**：打开提示词库时编辑器弹窗不再默认弹出。
- 窗口最小宽度计算更合理，根据 UI 比例和标题长度动态调整，不再使用固定值。

### 平台支持

Windows 版本经过完整测试与稳定性验证。macOS、Linux 与移动端仍在规划中。

### 下载

- **工百窗 Setup 0.0.5.exe**：安装版（含 x64 + arm64），推荐大多数用户使用
- **AI-Window-Portable-v0.0.5-win-x64.zip**：绿色便携版（x64），解压即用，数据存 exe 同级目录

---

## v0.0.4 — 2026-07-06

### 新功能

- **标签栏与底栏抽屉式收起**
  标签栏默认收起为一条细线，悬停展开，释放更多纵向空间。底栏同样默认隐藏在视口外，仅露出红色手柄，点击展开应用网格与标签管理。

- **语音引擎与模型管理**
  设置面板支持 whisper-cli 引擎和识别模型的独立下载与卸载，下载状态准确持久化，不再出现"已下载仍提示下载"的问题。

- **屏蔽规则管理**
  新增屏蔽规则设置分区，支持按域名配置 CSS 隐藏或 JS 脚本，屏蔽 AI 平台的下载引导、升级横幅、原生弹窗等干扰元素。内置规则可禁用不可删除。

- **AI 切换器智能分组**
  左上角 AI 切换器按使用频率分组，7 天内使用过的排在"常用 AI"，其余归入"其他 AI"。点击自定义 AI Provider 可直接打开或创建对话窗口。

- **对话窗口密度与代码主题**
  自定义 AI 对话窗口支持三档密度（紧凑 / 舒适 / 宽松），代码块支持深色 / 浅色两种高亮主题。

- **历史搜索增强**
  历史窗口支持消息级编辑与删除、对话标题编辑、展示层去重（相邻重复消息合并显示）。导入支持 JSON、DeepSeek 导出、Markdown 三种格式。

- **关闭行为可选**
  关闭按钮支持"直接关闭"和"最小化到托盘"两种模式，选择托盘模式后可通过快捷键随时恢复窗口。

- **三档 UI 缩放**
  外观设置新增紧凑 / 中档 / 大号三档 UI 比例，全局控制字号、间距与组件大小，适配不同屏幕和视力需求。

### 体验优化

- 录音指示器升级为实时音量波形可视化，录音过程直观可见。
- 录音超时 60 秒自动结束，防止热键 keyup 丢失导致无限录音。
- 后台语音上屏时自动备份并恢复用户原剪贴板内容，不干扰剪贴板。
- 后台录音增加 10 秒看门狗保护，异常挂起时自动超时结束。
- 自定义 AI 对话窗口支持 F11 最大化、F12 置顶快捷键，textarea 聚焦时同样生效。
- 自定义对话窗口支持默认提示词，新建对话时自动作为首条消息发送。
- 自定义对话窗口支持会话导出（MD / JSON），顶栏显示今日 token 用量。
- 历史窗口支持 F11 最大化，集成窗口大小调整手柄。
- 侧边栏在宽屏下支持折叠展开，窄屏保持抽屉式。

### 修复与改进

- 修复 whisper-cli 下载状态每次打开设置都重置为"未下载"的同步问题。
- 修复模型下载成功后切换模型再切回仍显示下载按钮的问题。
- 修复全屏状态退出后未持久化导致重启还原异常的问题。
- 修复预览窗首次创建时状态消息丢失的问题。
- 窗口关闭时完整保存 bounds、置顶状态、全屏状态三个字段。
- 底层代码进一步模块化，新增窗口配置构建器、权限管理器、平台动作封装等独立模块。

### 平台支持

目前仅提供 Windows 版本，macOS、Linux 与移动端已在规划中。

### 备注

开发环境暂缺，预计七月十五号以后恢复更新，期间尽可能收集 bug，以及筹备多窗口、组件黑白名单相关功能。

---

## v0.0.3 — 2026-07-02

### 新功能

- **智能干扰屏蔽**
  自动隐藏 AI 平台下载引导、升级横幅和原生弹窗（confirm/alert/prompt）。通过 CSS 隐藏、JS 干预与 MutationObserver 持续清理动态渲染内容，各窗口规则独立管理。

- **发送前提示词预览**
  Enter 发送时拦截，弹出预览浮层展示注入后的完整文本（前缀 + 正文 + 后缀），确认后才会发送，避免误发。

- **桌面/移动 UA 自动切换**
  每个平台独立配置桌面端与移动端 UA，窗口变窄或变宽时自动切换对应预设，无需手动调整。

- **多引擎语音识别**
  内置离线引擎优先，自动回退到命令行引擎、云端 API 或本地识别软件。多窗口场景下识别结果精准路由到对应窗口。

- **网页空间导航**
  使用方向键或手柄摇杆在页面元素间移动，配合虚拟光标操作。`Ctrl+G` 随时开关。

- **快捷键一键查阅**
  按 `` ` `` 或 `?` 呼出快捷键说明面板，所有操作一目了然。

### 体验优化

- 平台主题色可自定义，底栏切换器同步显示色点，平台辨识更直观。
- 设置面板重构为 10 个清晰分区，查找选项路径更短。
- 主题支持浅色、深色、跟随系统三种模式，实时响应系统外观变化。
- 语音权限采用双重检查机制，避免权限流程异常导致录音失败。
- 录音优先使用渲染进程原生能力，不再依赖 ffmpeg，跨平台部署更轻量。
- 代理支持系统代理、直连、自定义三种模式，并提供认证与地址绕过。
- 设备预设改为持久化存储，设置界面内可直接编辑，重启不丢失。

### 修复与改进

- 修复 F11、F12 在页面聚焦时失效的问题，全局快捷键更可靠。
- 网页弹窗采用三层拦截机制，阻止骚扰弹窗同时正常保留登录弹窗。
- 修复全屏覆盖后置顶状态丢失，现已自动恢复到最高级别置顶。
- 所有硬编码暗色背景统一使用 CSS 变量，主题适配更一致。
- 底层代码大幅重构：主界面拆分为 7 个子模块，设置面板拆分为 10 个独立部分，接口与类型定义按领域切分，为后续功能扩展筑牢基础。

### 平台支持

目前仅提供 Windows 版本，macOS、Linux 与移动端已在规划中。

---

## v0.0.2 — 2026-06-29

### 核心新功能

- **全局后台语音输入**：`Alt+V` 一键唤起，悬浮预览窗实时显示，支持自动回车发送
- **独立窗口体系**：提示词库、搜索历史均改为独立窗口，支持跨窗口一键注入
- **窗口热键**：`F11` 全屏/还原，`F12` 置顶/取消，主窗口与自定义窗口分别管理
- **网络代理**：支持系统代理/自定义代理，HTTP/SOCKS5，连通性一键测试

### 体验优化

- 标签栏默认折叠为呼吸灯效果，悬停展开，使用应用主题色
- 左上角 AI 切换器自适应底栏应用，点击空白收起
- 底栏展开时点击网页空白自动收起，中央 50% 区域禁止拖拽
- 窗口边框减薄 60%，默认浅色模式，支持跟随系统
- 登录痕迹自动保存，Web 对话数据主动捕获落库

### 修复改进

- 主窗口关闭彻底退出所有进程
- 防止重复启动多个实例
- 切换窗口时页面不再闪烁
- 大量代码结构重构，提升稳定性

### 平台支持

仅 Windows（Mac/Linux/移动端规划中）

---

## v0.0.1 — 2026-06-28

首个公开版本，支持 Windows 平台。macOS / Linux / 移动端规划中。

### 核心功能

- 全局热键体系：Alt+Space 一键呼出、Alt+Q 划词提问、Alt+V 后台语音
- 后台语音输入：基于 whisper.cpp 本地识别，不呼窗直接提问
- 四态窗口系统：全屏 / 侧边 / 预览 / 隐藏，适应不同场景
- 多网页挂载：webview 原生聚合，支持自定义任意网页

### 系统要求

- Windows 10 1903 及以上
