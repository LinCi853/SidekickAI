# 更新日志

---

## v0.2.0 — 2026-09-10

> 本版本重点：安装器整体重写为 Tauri 2 单文件自解压，打包体积近乎减半，并完成开源前清理。

### 新增

- **全新安装向导（Tauri 2 + React）**
  - 外层向导由 Electron 改为 Tauri 2 + React，载荷使用 7z solid 双架构归档。
  - 产出单文件安装包：向导 exe + `payload.7z` + `7zr.exe` + 28 字节 footer（`SKPAYLD1` 自解压格式）。
  - 智能 UAC 提权：根据安装模式决定是否提权，提权后不再重放已答页面。
  - 卸载流程与主应用恢复路径打通，备份导出格式统一。

- **安装清单单源化**
  - 安装器清单由 `electron/modules/builtin-module-data.ts` 单一数据源投影生成，构建期固化，避免清单与代码漂移。
  - 安装组件与功能开关拆分，支持协议逐项勾选。

- **设备绑定加密备份**
  - 备份文件使用 AES-256-GCM + PBKDF2（密码 + 设备 ID 作盐）加密，增强导出数据的可携带性与安全性。
  - 新增 `install-config` 种子机制：安装时写入配置，主程序首启读取并应用。

- **插件可扩展性**：引入模块清单与能力注册基础设施。

### 修复

- **窗口状态还原**
  - 使用 `normalBounds` 而非 `bounds` 还原创始尺寸，解决最大化状态还原异常。
  - 还原前校验记录坐标是否落在当前显示器范围内，避免窗口出现在屏幕外。
  - 延迟到 `show` 之后再最大化，消除窗口不可见问题；`moveTop()` 避免高级面板被主窗口遮挡。

- **数据导入稳定性**
  - 导入期间阻止应用退出；先销毁窗口再关闭 SQLite，避免退出清理时崩溃。
  - 在 `destroyed` 回调前捕获 `webContents` id，避免访问已销毁对象。
  - 抽出共享导入守卫模块替代零散 `require()` 判断。

- **依赖修复**：修复 `file-uri-to-path` 缺失；`clearAllData` 全量清理遗漏。

### 性能与体积

将渲染层依赖降为 `devDependencies`（electron-builder 会自动排除其依赖树），并移除白板插件中从未被引用的 `deps` 死重：

| 产物 | 优化前 | 优化后 |
| --- | --- | --- |
| 绿色包 | 500 MB | 239 MB |
| 安装包 | 249 MB | 119 MB |
| `app.asar` | 156 MB | 29 MB |

### 重构

- 主进程：IPC 安全包装、窗口广播、store 路径基础设施抽公共模块。
- 渲染层：默认配置改为中央路由（`default-config.ts`）；webview 快捷键改为 descriptor 驱动路由；抽取通用 hook 与组件。
- 清理废弃资源与移动端残留代码。

### 开源准备

- 清除历史中的内部工具元数据与一次性调试脚本，作者身份统一为 noreply 地址。
- 补齐许可合规（README 增加 7-Zip / LGPL-2.1-or-later 署名）。
- 新增 `.editorconfig`、`.gitattributes`、`CONTRIBUTING.md`、`SECURITY.md`、CI 与 Issue/PR 模板。

### 安全修复

- **zip 解压加固（zip-slip / 符号链接覆盖）**
  - 新增 `electron/utils/safe-zip.ts`：统一的不可信 zip 解压入口，校验条目名（拒绝绝对路径、盘符、`..` 穿越、控制字符）、确认落盘路径在目标目录之内、目标或其父目录为符号链接时拒绝、跳过 zip 内携带的 symlink 条目、限制条目数与单条体积。
  - 接入点：`electron/store/backup-restore.ts`（用户导入的备份）与 `electron/utils/zip-extractor.ts`（模型包解压）。
  - 依赖 `adm-zip` 由 `^0.5.16` 升级至 `^0.6.0`（修复「构造 zip 触发 4GB 内存分配」，GHSA-xcpc-8h2w-3j85）；
    「解压跟随目标端符号链接」（GHSA-vwc7-r8mq-g2x9）上游至 0.6.0 仍未修复，由本层的路径与符号链接校验兜住。
  - 新增 12 项防护单测（`electron/utils/safe-zip.test.ts`），此前该路径零覆盖。

### 验证

- 类型检查：`npm run typecheck` 通过。
- 单元测试：`npm test` 24 个文件 / 259 个用例通过。
- 构建：`npm run build` 通过。

---

## v0.1.0 — 2026-08-14

> 本版本重点：冻结状态下可见 HTML 文本的精确选择与复制。

### 关键修复

- **恢复 DeepSeek 等 SPA 的 glyph 文本层提取**
  - `GLYPH_EXTRACT_SCRIPT` 改用 `String.raw`，避免正则转义在脚本生成时失效。
  - 不再把 `user-select`、`pointer-events`、视觉 mask 或事件捕获覆盖层误判为文本不可见。
  - 根 `html/body` 的 `overflow:hidden` 使用 visual viewport 裁剪；避免零高 body 将固定定位 SPA 正文全部过滤。
  - 保留 transform 元数据，但基于 `Range.getClientRects()` 的坐标已经包含 CSS transform，正文不再因此被排除。

- **降低 DOMSnapshot 降级路径的错位风险**
  - 同时支持四值 rectangle 与八值 quad 边界格式。
  - 复用 glyph 提取到的视口、缩放、滚动与嵌套滚动元数据。
  - 按视觉行聚类 text boxes，避免每个 text box 被错误当成独立行。
  - 降级路径保持可复制，但精度为 text-box 级；字符级精确选择由 glyph 路径提供。

- **统一文本层可用性处理**
  - glyph 与 DOMSnapshot 文本层均可进入选择与复制路径。
  - `getTextLayerQuality()` 为 UI 提供 high / medium / none 状态。
  - 当没有任何可用文本层时才显示“当前页面无文本内容”。

### 测试验证

- **聚焦单测**：26/26 通过。
- **类型检查**：通过。
- **Electron POC**：28 项断言通过，覆盖 worker 暂停、嵌套滚动、可见性过滤、零高 body 与生产 glyph 脚本。
- **真实 DeepSeek IPC**：冻结返回 `quality: glyph`、39 个文本块、365 个 grapheme 边界。
- **代码质量**：`git diff --check` 通过。

### 文档更新

- 更新 `docs/test/v0.1.0-test-checklist.json`，区分 glyph 字符级选择与 DOMSnapshot text-box 级降级选择，并新增 DeepSeek SPA 回归项。

## v0.0.9 — 2026-08-09

> 本版本重点：AI 应用独立为全功能多标签浏览器窗口；三层栏（标签 / 导航 / 书签）；窗口最大化管理；导航历史与书签持久化。

### 新功能

- **AI 应用独立为浏览器窗口**
  将「独立为浏览器窗口」入口接入全新 BrowserView：Chrome 风格多标签页（默认最大化，还原尺寸 1280×800），支持固定标签、静音、标签搜索、最近关闭标签恢复（Ctrl+Shift+T）、跨窗口标签聚合。独立窗口与原主窗口共享登录态，关闭后标签自动迁回主窗口。

- **三层栏浏览器 UI**
  - 第一栏：标签页管理 + 窗口控制（新建 / 固定 / 静音 / 关闭 / 搜索）
  - 第二栏：导航 + 地址栏 + 工具（后退 / 前进 / 刷新 / 主页 / 站点权限 / 收藏 / 下载指示）
  - 第三栏：书签栏，支持新建、编辑、删除、重排序，按 Profile 隔离

- **窗口最大化管理**（WindowMaximizeManager）
  统一所有窗口的最大化 / 还原策略（主窗口、浏览器窗口按工作区适配），F11 全局切换全屏，最大化状态与还原尺寸持久化。

- **导航历史**：独立窗口内浏览历史按 Profile 记录，支持分页列表、关键词搜索、单条删除与清空。

- **站点权限控制**：浏览器窗口内可按站点查看 / 管理权限请求（摄像头、麦克风、通知等）。

- **累积链接**：AI 应用内点击新窗口打开的链接会在独立为浏览器窗口后自动转为标签页打开。

### 体验优化

- 窗口尺寸与位置持久化修复：最大化时保留还原尺寸，F11 还原后回到原有位置与大小。
- 下载记录支持打开所在文件夹 / 删除单条 / 清空，下载状态实时同步到第二栏指示器。
- 内置 AI 应用（如 DeepSeek）标记为不可删除、不可重命名，确保标签栏始终保有可用的保底入口。

### 问题修复

- 修复关闭独立浏览器窗口后主窗口标签丢失的问题（beforeunload 不可靠时由主进程保底迁移）。
- 修复「脱离过的应用无法再次打开」：新开标签时自动清除脱离标记。
- 修复启动时残留的脱离状态导致标签永久消失的问题。

---

## v0.0.8 — 2026-08-01

> 本版本重点：新增 Oxy Design System UI 模式；白板迁移至开源引擎；恢复截图到白板功能；设置面板独立窗口化。

### 重要更改

- **白板引擎迁移至开源组件**
  原白板组件采用专有许可证，存在商用合规风险。本版本整体迁移至开源引擎（MIT 许可），遗留数据自动迁移，多白板管理与白板侧边栏成为管理入口。

- **设置面板独立窗口化**
  主窗口内的侧滑设置面板改为独立窗口，左侧导航 + 右侧内容区布局，支持 ESC 关闭。设置分组为 5 类：外观与交互、AI 服务、网络与隐私、高级、关于。脱离窗口（Alt+Q 进阶面板、独立脱离窗口）保留侧滑设置面板，避免功能重复。

### 新功能

- **Oxy Design System 新 UI 模式**
  设置 → 外观新增「Oxy Design System」开关，开启后启用全新的现代化界面体系：
  - 全局品牌色跟随当前 AI 应用主题色，切换标签时品牌色自动跟随
  - 基于屏幕分辨率与宽高比自动计算 UI 尺寸，竖屏 / 超宽屏 / 高分屏自动适配
  - 强制亮色主题，颜色体系基于品牌色与背景色自动生成，保证对比度
  - 关闭后恢复经典版（手动主题模式 + 手动 UI 比例）

- **截图 AI 页面到白板**（恢复）
  右键顶栏标签页 →「页面操作」→「截图到白板」，将当前 AI 页面截图推送至进阶面板白板，自动滚动到视口。图片宽度超过 400px 时按比例缩放，随机偏移避免多张截图重叠。

### 体验优化

- **对比度系统性修复**：用户气泡、主按钮、应用切换器图标、提示词库标签、数据导出预设、供应商模型标签等 11 处「浅色背景 + 白字」场景对比度提升至 WCAG AA 标准。
- **快捷键冲突徽章对比度修复**：从「白字 + 琥珀底」改为亮色深棕字 / 暗色提亮琥珀 + 深字，双主题一致。
- **关于页版本号动态读取**：版本号从硬编码改为动态读取，与 `package.json` 保持一致，不再需要手动维护。

### 问题修复

- 修复切换标签时页面报错 `ERR_ABORTED` 的问题（SPA 内部导航触发重渲染导致）。
- 修复暗色主题下错误红色仍使用亮色值的问题。

### 开源合规

- README 新增商标声明：本项目不隶属于所聚合的任何 AI 服务提供商，仅使用平台名称首字母 + 自定义渐变色作为视觉标识，未内置 / 分发任何平台 Logo 或商标图形。
- README 新增开源依赖许可证清单。

---

## v0.0.7 — 2026-07-26

> 本版本重点：webview 反检测解决 AI 平台环境检测问题；进阶面板概念统一与专属设置；笔记 / 白板数据迁移至 SQLite 并补齐防抖自动保存；独立窗口体验全面统一。

### 重要更改

- **「AI 应用」→「高级面板」概念统一**
  原「AI 应用」概念全面重命名为「高级面板」，所有 UI 文案同步更新。

- **笔记 / 白板数据迁移至 SQLite**
  应用启动时自动将旧数据迁移到 SQLite，数据存储更可靠，查询更快。迁移后旧文件备份保留。

### 新功能

- **webview 反检测**
  在页面脚本执行前注入反检测逻辑，防止 AI 网站检测到 Electron / WebView 环境并限制功能：
  - 隐藏自动化环境信号
  - 补全浏览器特征对象与插件列表
  - 清理泄露 Electron 环境的全局变量
  - 将 webview 标签伪装为 iframe

  之前可能被 AI 平台检测并限制功能的网页，现在可正常使用。

- **进阶面板专属设置**
  Alt+Q 进阶面板新增独立设置入口：
  - 可选择默认打开的标签页（自定义对话 / 白板 / 灵感笔记）
  - 可控制白板侧边栏的显示 / 隐藏

- **Alt+Q 切换行为改进**
  窗口可见时再次按 Alt+Q 直接关闭窗口，下次重新打开时按「默认打开」设置路由到用户选择的标签页，行为更可预测。

- **Ctrl+1/2/3 标签快捷切换**
  进阶面板新增键盘快捷键：Ctrl+1 → 自定义对话、Ctrl+2 → 白板、Ctrl+3 → 灵感笔记。输入框聚焦时不触发。

- **可搜索下拉选择**
  模型选择器与 UA 预设选择升级为可搜索列表，支持实时过滤，配置多个供应商 / 模型时切换更高效。支持备选模型配置与主 / 备角色切换。

- **TTS 语音合成独立配置**
  AI 供应商新增 TTS / STT 配置（复用本供应商的接口合成语音或识别语音），默认关闭需显式开启，支持测试连接返回音频预览。

- **AI Provider 加密导出 / 导入**
  供应商配置支持加密导出为 `.sapp` 文件（密码加密），支持选择性导出和预览导入（不持久化，返回冲突列表供确认）。

- **提示词模板字段自动迁移**
  自动检测并迁移含旧字段的提示词模板到新格式。

### 体验优化

- **ESC / Ctrl+W 统一关窗**
  所有独立窗口统一键盘关窗行为：ESC 关闭窗口（有浮窗时先关浮窗，输入框聚焦时跳过）；Ctrl+W 直接关闭。

- **独立窗口体验统一**
  所有独立窗口（提示词库、历史、数据导出、进阶面板等）的标题栏、调整手柄、置顶 / 最大化按钮、窄屏适配全面统一。侧边栏宽度可拖拽调整并持久化。

- **笔记防抖自动保存**
  编辑后 800ms 自动保存，切换笔记或关闭窗口前同步保存，杜绝数据丢失。

- **白板防抖自动保存**
  编辑后 500ms 自动保存，关闭窗口前同步兜底保存。

- **API Key 显隐切换**
  供应商 API Key 输入框新增眼睛图标切换显隐。

---

## v0.0.6 — 2026-07-17

> 本版本重点：安装包按 CPU 架构拆分发行，单架构体积下降超 50%；并补齐多开、自启、数据迁移、缓存 / 下载管理等实用能力。

### 重要更改

- **安装包按架构拆分**：x64 与 arm64 各出一个独立安装包，单架构体积从 180 MB 降至 78 MB（-56.6%），下载更快、占用更小。arm64 包在 x64 系统上运行时会弹窗提示，引导下载正确版本。

### 新功能

- **AI 应用多开与拖拽排序**：同一 AI 平台支持同时打开多个独立实例（如同时登录两个豆包账号），通过「复制」快速克隆配置，登录状态互不干扰；切换器、底部栏、设置面板均支持拖拽调整顺序。
- **开机自启动**：设置面板「通用」分区新增「开机自启动」开关，附带「静默启动」选项，自启后隐藏到托盘不弹出主窗口。
- **跨设备数据迁移**：设置面板「关于」分区可导出 / 导入完整数据包（含对话记录、登录状态和个性化设置），新设备导入后无需重新登录。导出支持细粒度选项，避免冗余体积。
- **缓存与下载管理**：新增缓存清理（手动 + 每日 / 每周 / 每月自动），仅清缓存保留登录；文本 / 代码文件可下载到指定目录，支持每次询问或自动保存。
- **浏览器文件拖拽导入**：将文件拖入窗口即可自动注入当前 AI 应用，三层兜底策略兼容智谱清言等延迟挂载输入框的平台。
- **Alt+Space 触发阈值可自定义**：设置面板「热键」分区新增次数配置（默认 6 次，范围 3-20），时间窗口随阈值自动放大，避免误触恢复窗口位置。
- **弹窗白名单机制**：连续 3 次拒绝弹窗后自动提示加入白名单，避免反复拦截登录 / OAuth 弹窗。

### 体验优化

- 清理设置面板内冗余的描述性说明文字，保留危险操作的二次确认提示与动态状态反馈。
- 设置面板折叠区块（AI 应用、设备预设）标题样式统一，展开 / 收起箭头反馈更清晰。
- 全部页面更新至 v0.0.6 版本标识。

---

## v0.0.5 — 2026-07-14

> 上一版本的稳定化版本，修复多项已发现问题，补齐窗口管理、快捷键和界面体验短板。

### 新功能

- **首次启动引导**：首次打开时弹出分步引导窗口，介绍核心亮点、窗口类型、快捷键速查和快速设置；老用户可点击「跳过」直接进入。
- **顶栏按钮自定义**：设置面板新增「顶栏」分区，可在 6 组按钮（UA 切换、后退、前进、主页、主题切换、置顶）中按需显隐。
- **新增标签快捷键**：Ctrl+T 新建标签（默认打开 DeepSeek），Ctrl+W 关闭当前标签。
- **新增导航快捷键**：F4 后退、F5 刷新、F6 前进，作用于当前标签。
- **F12 置顶全窗口通用**：自定义对话、历史搜索、提示词库、AI 应用编辑、使用引导等所有窗口均支持 F12 切换置顶，与主窗口一致。

### 体验优化

- UA 切换按钮升级为三态循环（自动 → 移动端 → 桌面端），每种状态配独立图标。
- AI 应用管理视图精简：默认进入对话列表，卡片新增「已打开」徽标和「打开 / 编辑」双按钮。
- AI 应用编辑视图整体优化：顶栏新增置顶、设置按钮；模型选择器样式更清晰；发送按钮与输入框对齐到同一栏；添加窗口边框避免与背景融合。
- 自定义对话窗口发送按钮样式统一，颜色更醒目，hover 状态更稳定。
- 使用引导布局优化：从 5 页合并为 4 页，消除底部空白，始终显示「跳过」按钮。
- 快捷键速查表全面同步，补齐 Ctrl+T/W、F4/5/6、F10 等条目。

### 问题修复

- **修复窗口无法缩小**：上一版本中窗口只能放大不能缩小，现已恢复正常。
- **修复拖动一次后调整手柄失效**：拖动窗口边缘一次后手柄卡死的问题已修复。
- **修复网页崩溃后无法恢复**：使用一段时间后点击主页或刷新报错的问题已修复，崩溃后可自动恢复。
- **修复提示词库编辑器弹窗默认显示**：打开提示词库时编辑器弹窗不再默认弹出。
- 窗口最小宽度计算更合理，根据 UI 比例和标题长度动态调整，不再使用固定值。

### 平台支持

Windows 版本经过完整测试与稳定性验证。macOS、Linux 与移动端仍在规划中。

### 下载

- **工百窗 Setup 0.0.5.exe**：安装版（含 x64 + arm64），推荐大多数用户使用
- **AI-Window-Portable-v0.0.5-win-x64.zip**：绿色便携版（x64），解压即用，数据存 exe 同级目录

---

## v0.0.4 — 2026-07-06

### 新功能

- **标签栏与底栏抽屉式收起**
  标签栏默认收起为一条细线，悬停展开，释放更多纵向空间。底栏同样默认隐藏在视口外，仅露出红色手柄，点击展开应用网格与标签管理。

- **语音引擎与模型管理**
  设置面板支持 whisper-cli 引擎和识别模型的独立下载与卸载，下载状态准确持久化，不再出现"已下载仍提示下载"的问题。

- **屏蔽规则管理**
  新增屏蔽规则设置分区，支持按域名配置 CSS 隐藏或 JS 脚本，屏蔽 AI 平台的下载引导、升级横幅、原生弹窗等干扰元素。内置规则可禁用不可删除。

- **AI 切换器智能分组**
  左上角 AI 切换器按使用频率分组，7 天内使用过的排在"常用 AI"，其余归入"其他 AI"。点击自定义 AI Provider 可直接打开或创建对话窗口。

- **对话窗口密度与代码主题**
  自定义 AI 对话窗口支持三档密度（紧凑 / 舒适 / 宽松），代码块支持深色 / 浅色两种高亮主题。

- **历史搜索增强**
  历史窗口支持消息级编辑与删除、对话标题编辑、展示层去重（相邻重复消息合并显示）。导入支持 JSON、DeepSeek 导出、Markdown 三种格式。

- **关闭行为可选**
  关闭按钮支持"直接关闭"和"最小化到托盘"两种模式，选择托盘模式后可通过快捷键随时恢复窗口。

- **三档 UI 缩放**
  外观设置新增紧凑 / 中档 / 大号三档 UI 比例，全局控制字号、间距与组件大小，适配不同屏幕和视力需求。

### 体验优化

- 录音指示器升级为实时音量波形可视化，录音过程直观可见。
- 录音超时 60 秒自动结束，防止热键 keyup 丢失导致无限录音。
- 后台语音上屏时自动备份并恢复用户原剪贴板内容，不干扰剪贴板。
- 后台录音增加 10 秒看门狗保护，异常挂起时自动超时结束。
- 自定义 AI 对话窗口支持 F11 最大化、F12 置顶快捷键，textarea 聚焦时同样生效。
- 自定义对话窗口支持默认提示词，新建对话时自动作为首条消息发送。
- 自定义对话窗口支持会话导出（MD / JSON），顶栏显示今日 token 用量。
- 历史窗口支持 F11 最大化，集成窗口大小调整手柄。
- 侧边栏在宽屏下支持折叠展开，窄屏保持抽屉式。

### 修复与改进

- 修复 whisper-cli 下载状态每次打开设置都重置为"未下载"的同步问题。
- 修复模型下载成功后切换模型再切回仍显示下载按钮的问题。
- 修复全屏状态退出后未持久化导致重启还原异常的问题。
- 修复预览窗首次创建时状态消息丢失的问题。
- 窗口关闭时完整保存 bounds、置顶状态、全屏状态三个字段。
- 底层代码进一步模块化，新增窗口配置构建器、权限管理器、平台动作封装等独立模块。

### 平台支持

目前仅提供 Windows 版本，macOS、Linux 与移动端已在规划中。

### 备注

开发环境暂缺，预计七月十五号以后恢复更新，期间尽可能收集 bug，以及筹备多窗口、组件黑白名单相关功能。

---

## v0.0.3 — 2026-07-02

### 新功能

- **智能干扰屏蔽**
  自动隐藏 AI 平台下载引导、升级横幅和原生弹窗（confirm/alert/prompt）。通过 CSS 隐藏、JS 干预与 MutationObserver 持续清理动态渲染内容，各窗口规则独立管理。

- **发送前提示词预览**
  Enter 发送时拦截，弹出预览浮层展示注入后的完整文本（前缀 + 正文 + 后缀），确认后才会发送，避免误发。

- **桌面/移动 UA 自动切换**
  每个平台独立配置桌面端与移动端 UA，窗口变窄或变宽时自动切换对应预设，无需手动调整。

- **多引擎语音识别**
  内置离线引擎优先，自动回退到命令行引擎、云端 API 或本地识别软件。多窗口场景下识别结果精准路由到对应窗口。

- **网页空间导航**
  使用方向键或手柄摇杆在页面元素间移动，配合虚拟光标操作。`Ctrl+G` 随时开关。

- **快捷键一键查阅**
  按 `` ` `` 或 `?` 呼出快捷键说明面板，所有操作一目了然。

### 体验优化

- 平台主题色可自定义，底栏切换器同步显示色点，平台辨识更直观。
- 设置面板重构为 10 个清晰分区，查找选项路径更短。
- 主题支持浅色、深色、跟随系统三种模式，实时响应系统外观变化。
- 语音权限采用双重检查机制，避免权限流程异常导致录音失败。
- 录音优先使用渲染进程原生能力，不再依赖 ffmpeg，跨平台部署更轻量。
- 代理支持系统代理、直连、自定义三种模式，并提供认证与地址绕过。
- 设备预设改为持久化存储，设置界面内可直接编辑，重启不丢失。

### 修复与改进

- 修复 F11、F12 在页面聚焦时失效的问题，全局快捷键更可靠。
- 网页弹窗采用三层拦截机制，阻止骚扰弹窗同时正常保留登录弹窗。
- 修复全屏覆盖后置顶状态丢失，现已自动恢复到最高级别置顶。
- 所有硬编码暗色背景统一使用 CSS 变量，主题适配更一致。
- 底层代码大幅重构：主界面拆分为 7 个子模块，设置面板拆分为 10 个独立部分，接口与类型定义按领域切分，为后续功能扩展筑牢基础。

### 平台支持

目前仅提供 Windows 版本，macOS、Linux 与移动端已在规划中。

---

## v0.0.2 — 2026-06-29

### 核心新功能

- **全局后台语音输入**：`Alt+V` 一键唤起，悬浮预览窗实时显示，支持自动回车发送
- **独立窗口体系**：提示词库、搜索历史均改为独立窗口，支持跨窗口一键注入
- **窗口热键**：`F11` 全屏/还原，`F12` 置顶/取消，主窗口与自定义窗口分别管理
- **网络代理**：支持系统代理/自定义代理，HTTP/SOCKS5，连通性一键测试

### 体验优化

- 标签栏默认折叠为呼吸灯效果，悬停展开，使用应用主题色
- 左上角 AI 切换器自适应底栏应用，点击空白收起
- 底栏展开时点击网页空白自动收起，中央 50% 区域禁止拖拽
- 窗口边框减薄 60%，默认浅色模式，支持跟随系统
- 登录痕迹自动保存，Web 对话数据主动捕获落库

### 修复改进

- 主窗口关闭彻底退出所有进程
- 防止重复启动多个实例
- 切换窗口时页面不再闪烁
- 大量代码结构重构，提升稳定性

### 平台支持

仅 Windows（Mac/Linux/移动端规划中）

---

## v0.0.1 — 2026-06-28

首个公开版本，支持 Windows 平台。macOS / Linux / 移动端规划中。

### 核心功能

- 全局热键体系：Alt+Space 一键呼出、Alt+Q 划词提问、Alt+V 后台语音
- 后台语音输入：基于 whisper.cpp 本地识别，不呼窗直接提问
- 四态窗口系统：全屏 / 侧边 / 预览 / 隐藏，适应不同场景
- 多网页挂载：webview 原生聚合，支持自定义任意网页

### 系统要求

- Windows 10 1903 及以上