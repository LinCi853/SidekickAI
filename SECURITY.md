# 安全策略

## 支持范围

| 版本 | 是否接收安全更新 |
| --- | --- |
| 最新发布版 | ✅ |
| 更早版本 | ❌（请先升级到最新版） |

## 报告漏洞

**请勿通过公开 Issue 报告安全漏洞。**

请使用 GitHub 的私密漏洞报告通道：

1. 打开本仓库的 **Security** 标签页
2. 点击 **Report a vulnerability**
3. 填写复现步骤、影响版本、影响面与（如有）PoC

> 若仓库尚未开启该功能，需要维护者在 **Settings → Code security → Private vulnerability reporting** 中启用。

## 我们会怎么处理

- 收到报告后确认并评估影响，必要时与报告者协作复现
- 修复以补丁版本发布，并在 `CHANGELOG.md` 中记录（在细节公开后）
- 如你同意，会在发布说明中致谢

## 范围说明

本项目是**本地桌面应用**，请重点关注的类别：

- 数据落盘/导入导出（`electron/store/`：备份加密、SQLite、配置文件读写）
- 进程与权限边界（`electron/`：IPC 校验、window/webview 隔离、UAC 提权流程）
- 安装器（`installer-tauri/`：载荷解压路径穿越、提权顺序、临时文件权限）
- 渲染层注入与消毒（`src/`：DOM 注入、URL 白名单、webview 阻止规则）

以下**不属于**漏洞：

- 需要用户已具备本机管理员权限才能触发的行为
- 用户自行导入的第三方 AI 平台页面本身的问题
- 依赖项自身的漏洞（请直接向上游报告，同时欢迎在本仓库提 Issue 提示升级）

## 依赖与许可

第三方组件的安全与许可信息见 `README.md` 的「开源依赖」章节。
